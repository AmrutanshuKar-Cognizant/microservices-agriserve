server:
  port: 8083

spring:
  application:
    name: COMPLIANCE-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/complianceservice_db
    username: root
    password: "@Ilysh0312"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  config:
    import: optional:configserver:http://localhost:8888

#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka/
#    fetch-registry: true
#    register-with-eureka: true
#  instance:
#    prefer-ip-address: true

internal:
  service:
    key: MyClientSecretKey123!